# PitchForge - AI Monetization Strategist

## Overview
PitchForge is an AI-powered platform that converts student project descriptions into actionable monetization strategies. It generates target personas, pricing models, and cold pitches instantly.

## Technology Stack
* Frontend: React (Vite), Tailwind CSS
* AI Integration: Cohere API
* Styling: Lucide React Icons & Custom CSS

## Local Setup Instructions
1. Clone the repository.
2. Run `npm install` to install dependencies.
3. Create a `.env` file in the root directory.
4. Add your API key: `VITE_COHERE_API_KEY=your_key_here`
5. Run `npm run dev` to start the local server.

## VibeHack AI Disclosure
* **Tools Used:** * **Core Engine:** Cohere API 
  * **Development Assistants:** Claude, Gemini Assistant, GitHub Copilot CLI
* **How they were used:** * **Cohere API** acts as the core backend engine, processing user inputs via a strict system prompt to return structured JSON data for the UI.
  * **Claude & Gemini** were used during development to architect the React component structure, generate the Tailwind CSS styling, and refine the UI aesthetics.
  * **GitHub Copilot CLI** was used to debug and inject specific micro-animations.
* **AI-Assisted Parts:** The application logic, UI components, and API fetch calls were built with AI assistance. The conceptual architecture, the "DEMO" failsafe bypass, prompt engineering, and product flow were human-directed.

## Team
* **Team Name:** Binary Baniyas
* **Members:** Yash Singhal, Aditya Mittal
